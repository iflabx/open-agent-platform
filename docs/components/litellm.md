# LiteLLM

> 类型：模型网关
>
> 复用规则：稳定复用的组件说明页

## 当前定位

`LiteLLM` 是当前方案的统一模型网关，向上提供统一接口，向下路由到 `vLLM` 及对应模型池。

## 主要职责

- 统一模型访问接口
- 模型路由、限流、配额和熔断
- 请求日志、成本统计和故障切换

## 与其他组件关系

- 上游对接主平台、自研服务和 Agent 运行时
- 下游连接 `vLLM`

## 边界

- 不承载知识库
- 不负责工作流编排
- 不替代 API 网关
