# vLLM

> 类型：推理服务
>
> 复用规则：稳定复用的组件说明页

## 当前定位

`vLLM` 是当前方案的统一推理服务底座，负责承载企业内部主模型、代码模型和安全模型的在线推理能力。

## 主要职责

- 模型加载与 GPU 资源利用
- 高并发推理与吞吐优化
- 作为 `LiteLLM` 后端推理服务

## 与其他组件关系

- 上游统一由 `LiteLLM` 调度
- 下游承载 `Qwen` 模型族
- 不直接暴露给业务应用

## 边界

- 不承担业务编排
- 不承担权限治理
- 不承担知识检索
