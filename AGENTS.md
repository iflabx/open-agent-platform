# 仓库规则

## 公开站点展示规则

- 公开网站始终只展示一个当前状态，不展示内部方案版本号。
- `V3`、`V4` 这类内部迭代标识不得出现在公开页面标题、导航名称、卡片文案、正文说明和公开路由中。
- 公开一级入口固定使用无版本名称，例如 `方案`、`组件`、`开源状态`。
- 主方案始终发布到 `/framework`，组件清单始终发布到 `/components`。
- 组件页、协议页、标准页、规范页采用稳定路径复用，不新增公开的版本化页面。

## 站点标题规则

- 首页主标题、站点标题和相应公开标题统一使用 `企业级智能体开源系统方案`。
